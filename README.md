# Magic RAG

## Install (Windows CMD)

Run these commands in Windows Command Prompt (`cmd.exe`):

```cmd
gh repo clone adidaniel1000/magic-rag
cd magic-rag
setup\setup.bat
startMagicRagUI.bat
```

If Windows gives you permission issues, see [Manual Setup.md](<docs/Manual Setup.md>).
